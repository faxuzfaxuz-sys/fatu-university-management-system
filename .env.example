# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/fatu

# Use a long random value in every environment.
JWT_SECRET=change-this-to-a-long-random-secret

# Optional: enables POST /api/seed when the header x-seed-secret matches.
# Leave empty or remove it after initial setup.
SEED_SECRET=